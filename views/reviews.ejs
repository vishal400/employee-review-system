<div class="container mt-5 review-container">
    <h2 class="text-center mb-4">Reviews</h2>

    <!-- Assigned Reviews Section -->
    <div class="row review-section">
        <div class="col-md-12">
            <h4>Assigned Reviews</h4>
            <ul class="list-group">
                <!-- If no assigned reviews -->
                <% if (assignedReviews.length === 0) { %>
                <li class="list-group-item">No assigned reviews available</li>
                <% } else { %> <% assignedReviews.forEach(function(review) { %>
                <li class="list-group-item">
                    <form
                        action="/submitReview"
                        method="post"
                        class="review-form"
                    >
                        <div class="form-group">
                            <input
                                type="hidden"
                                name="from"
                                value="<%= review.from %>"
                            />
                            <label>From: <%= review.from %></label>
                            <input
                                type="text"
                                class="form-control"
                                name="message"
                                placeholder="Enter your review"
                                required
                            />
                        </div>
                        <button
                            type="submit"
                            class="btn btn-primary btn-review"
                        >
                            Submit Review
                        </button>
                    </form>
                </li>

                <% }); %> <% } %>
            </ul>
        </div>
    </div>

    <!-- Received Reviews Section -->
    <div class="row review-section">
        <div class="col-md-12">
            <h4>Received Reviews</h4>
            <ul class="list-group">
                <!-- If no received reviews -->
                <% if (reviews.length === 0) { %>
                <li class="list-group-item">No received reviews available</li>
                <% } else { %>
                <!-- If received reviews exist, loop through them -->
                <% reviews.forEach(function(review) { %>
                <li class="list-group-item">
                    From: <%= review.from %><br />Message: <%= review.message %>
                </li>
                <% }); %> <% } %>
            </ul>
        </div>
    </div>
</div>
